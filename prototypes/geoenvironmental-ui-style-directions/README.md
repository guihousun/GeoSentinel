# 地缘环境智能计算平台 UI 视觉方向

本目录只用于 Gate 1 视觉方向评审，不修改正式 `web/` 页面。

- `?direction=atlas`：方向 01「测绘档案」
- `?direction=ledger`：方向 02「证据台账」
- `?direction=polar`：方向 03「极夜测绘」

三套方向保持相同的左侧任务导航、中央任务对话和右侧全球监测布局。界面内事件仅用于真实中文长度的排版预览，并明确标注为非实时数据。
